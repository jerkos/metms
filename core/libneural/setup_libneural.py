from distutils.core import setup, Extensionmodule = Extension('_libneural',                    sources = ['libneural.cpp', 'libneural_wrap.cxx'])                    #include_dirs=['.'],                    #libraries=['C:/MinGW/include'])setup (name = 'neuron_app',       version = '1.0',       description = 'This package allow to create all possible chemical formulas given a certain mass',       ext_modules = [module])       #,        #py_modules=['generator'])